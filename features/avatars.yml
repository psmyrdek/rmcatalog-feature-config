description: Show avatars in the list of users
tags:
  - all

bucketBy: userId

environments:
  preview:
    rules:
      - key: "1"
        segments: "*"
        percentage: 100

  development:
    rules:
      - key: "1"
        segments: "*"
        percentage: 100

  staging:
    rules:
      - key: "1"
        segments: "*"
        percentage: 100

  production:
    rules:
      - key: "1"
        segments: "*"
        percentage: 50
